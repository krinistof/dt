syntax = "proto3";

package log.v1;

// Collects console logs from clients for debugging
service LogCollectorService {
  // Endpoint for clients to send
  rpc Log(LogRequest) returns (LogResponse);
}

message LogRequest {
  string message = 1;
}

message LogResponse {}
